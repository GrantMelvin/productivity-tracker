name: Docker Image CI

on:
  push:
    branches: [ main, genai ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run build command
        run: |

          echo "Using API Key: ${{ secrets.DOCKERUSER }}"
          echo "Using API Secret: ${{ secrets.DOCKERPASS }}"

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build the Docker image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/productivity_tracker:latest .

      - name: Push the Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/productivity_tracker:latest